export type Product = {
  code?: string;
  name?: string;
  url?: string;
  imageProxied?: string;
  imageUrl?: string;
  description?: string;
  specsBullets?: string[];
  pdfUrl?: string;
  imageUrl?: string;        // raw/original
  imageProxied?: string;    // same-origin proxied url
  pdfKey?: string;   // NEW
  category?: string;
};


 
  

